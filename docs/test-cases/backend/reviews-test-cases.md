# Manual Test Cases - Reviews Module

| ID | Module | Title | Priority | Preconditions | Steps | Test Data | Expected Result | Status |
| -- | ------ | ------ | --------- | -------------- | ----- | --------------- | ------------------ | ------ |
| TC-REV-001 | Reviews | Create review successfully | High | User authenticated, book exists in database | 1. Send POST /api/reviews with valid token and review data<br>2. Verify HTTP 201 response<br>3. Verify success is true<br>4. Verify review is created with correct data | Valid token, bookId: valid_book_id, comment: "Excelente lectura", rating: 5, title: "Increíble" | HTTP 201, success: true, message: "Reseña creada exitosamente", review with provided data | |
| TC-REV-002 | Reviews | Reject creating without authentication | High | No authentication token provided | 1. Send POST /api/reviews without token<br>2. Verify HTTP 401 response<br>3. Verify authorization error message | bookId: valid_book_id, rating: 5, comment: "test", no token | HTTP 401, success: false, message: "No autorizado: se requiere un token de autenticación" | |
| TC-REV-003 | Reviews | Reject creating if rating is not between 1 and 5 | High | User authenticated, book exists | 1. Send POST /api/reviews with rating outside 1-5 range<br>2. Verify HTTP 400 response<br>3. Verify rating validation error message | Valid token, bookId: valid_book_id, rating: 6, comment: "test" | HTTP 400, success: false, message: "La calificación debe estar entre 1 y 5" | |
| TC-REV-004 | Reviews | Reject creating duplicate review for same book | High | User authenticated, user already reviewed this book | 1. Create first review for book<br>2. Attempt to create second review for same book<br>3. Verify HTTP 400 response<br>4. Verify duplicate review error message | Valid token, bookId: valid_book_id, first review, second review attempt | HTTP 400, success: false, message: "Ya has reseñado este libro. Puedes editar tu reseña existente si deseas actualizarla." | |
| TC-REV-005 | Reviews | Reject creating if book does not exist | High | User authenticated, book does not exist in database | 1. Send POST /api/reviews with non-existent bookId<br>2. Verify HTTP 404 response<br>3. Verify book not found error message | Valid token, bookId: non_existent_uuid, rating: 5, comment: "test" | HTTP 404, success: false, message: "Libro no encontrado" | |
| TC-REV-006 | Reviews | Get reviews by book successfully | High | Book exists in database, reviews exist for book | 1. Create reviews for book<br>2. Send GET /api/reviews/book/:bookId<br>3. Verify HTTP 200 response<br>4. Verify success is true<br>5. Verify reviews array and summary data | bookId: valid_book_id, multiple reviews | HTTP 200, success: true, data array with reviews, summary with averageRating and ratingDistribution | |
| TC-REV-007 | Reviews | Get empty reviews list for book with no reviews | Medium | Book exists in database, no reviews exist | 1. Send GET /api/reviews/book/:bookId<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify empty array and averageRating 0 | bookId: valid_book_id | HTTP 200, success: true, data: [], summary.averageRating: 0 | |
| TC-REV-008 | Reviews | Update own review successfully | High | User authenticated, review exists and belongs to user | 1. Create review<br>2. Send PUT /api/reviews/:reviewId with updated data<br>3. Verify HTTP 200 response<br>4. Verify review is updated with new data | Valid token, reviewId: own_review_id, updated rating: 5, updated comment: "Actualizado" | HTTP 200, success: true, message: "Reseña actualizada exitosamente", updated rating and comment | |
| TC-REV-009 | Reviews | Reject updating another user's review (Security) | High | User authenticated, attempting to update different user's review | 1. User A creates review<br>2. User B attempts to update User A's review<br>3. Verify HTTP 404 response<br>4. Verify permission error message | User B token, reviewId: belongs_to_user_A | HTTP 404, success: false, message: "Reseña no encontrada o no tienes permiso para editarla" | |
| TC-REV-010 | Reviews | Delete own review successfully | High | User authenticated, review exists and belongs to user | 1. Create review<br>2. Send DELETE /api/reviews/:reviewId with valid token<br>3. Verify HTTP 200 response<br>4. Verify review is deleted from database | Valid token, reviewId: own_review_id | HTTP 200, success: true, message: "Reseña eliminada exitosamente", review no longer exists in book reviews | |
| TC-REV-011 | Reviews | Reject deleting another user's review (Security) | High | User authenticated, attempting to delete different user's review | 1. User A creates review<br>2. User B attempts to delete User A's review<br>3. Verify HTTP 404 response<br>4. Verify permission error message | User B token, reviewId: belongs_to_user_A | HTTP 404, success: false, message: "Reseña no encontrada o no tienes permiso para eliminarla" | |
| TC-REV-012 | Reviews | Handle XSS in comments (Security) | Medium | User authenticated, book exists | 1. Send POST /api/reviews with script tag in comment<br>2. Verify HTTP 201 response<br>3. Verify comment is stored as text without execution | Valid token, bookId: valid_book_id, comment: "<script>alert('xss')</script> Muy buen libro!" | HTTP 201, success: true, comment stored as text without script execution | |
| TC-REV-013 | Reviews | Handle invalid UUID format for review ID | Medium | User authenticated, invalid UUID format provided | 1. Send DELETE /api/reviews/:invalidUuid with valid token<br>2. Verify HTTP 400+ response<br>3. Verify error message | Valid token, reviewId: "not-a-valid-uuid" | HTTP 400+, success: false, controlled error message | |
| TC-REV-014 | Reviews | Handle SQL injection in comments (Security) | Medium | User authenticated, book exists | 1. Send POST /api/reviews with SQL injection attempt in comment<br>2. Verify HTTP 201 response<br>3. Verify comment is stored as text without SQL execution | Valid token, bookId: valid_book_id, comment: "'; DROP TABLE reviews; --" | HTTP 201, success: true, comment stored as text without SQL execution | |
| TC-REV-015 | Reviews | Get user's own reviews | High | User authenticated, user has created reviews | 1. Create multiple reviews for user<br>2. Send GET /api/reviews/user with valid token<br>3. Verify HTTP 200 response<br>4. Verify only user's reviews are returned<br>5. Verify summary data is calculated correctly | Valid token, user with multiple reviews | HTTP 200, success: true, data array contains user's reviews only, summary with averageRating | |
| TC-REV-016 | Reviews | Get all reviews (public endpoint) | Medium | Reviews exist in database | 1. Send GET /api/reviews without authentication<br>2. Verify HTTP 200 response<br>3. Verify reviews array with pagination metadata<br>4. Verify reviews include user and book data | No authentication required | HTTP 200, success: true, data array with reviews, meta with pagination, reviews include user and book details | |
