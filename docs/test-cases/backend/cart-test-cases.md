# Manual Test Cases - Cart Module

| ID | Module | Title | Priority | Preconditions | Steps | Test Data | Expected Result | Status |
| -- | ------ | ------ | --------- | -------------- | ----- | --------------- | ------------------ | ------ |
| TC-CART-001 | Cart | Add book to cart successfully | High | User authenticated with valid token, book exists with available stock | 1. Send POST /carts/add with valid token, bookId and quantity<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify book is added with correct quantity | bookId: valid_book_id, quantity: 2 | HTTP 200, success: true, message: "Libro agregado al carrito exitosamente", item with correct bookId and quantity | |
| TC-CART-002 | Cart | Add book without quantity (default to 1) | Medium | User authenticated with valid token, book exists with available stock | 1. Send POST /carts/add with valid token and bookId only<br>2. Verify HTTP 200 response<br>3. Verify quantity defaults to 1 | bookId: valid_book_id (no quantity) | HTTP 200, success: true, item.quantity: 1 | |
| TC-CART-003 | Cart | Accumulate quantity if book already exists in cart | Medium | User authenticated, book exists, book already in cart with quantity | 1. Add book to cart with quantity 1<br>2. Add same book again with quantity 2<br>3. Verify total quantity is accumulated | First: bookId: valid_book_id, quantity: 1<br>Second: bookId: valid_book_id, quantity: 2 | HTTP 200, success: true, final quantity: 3 (1 + 2) | |
| TC-CART-004 | Cart | Reject adding without authentication | High | No authentication token provided | 1. Send POST /carts/add without token<br>2. Verify HTTP 401 response<br>3. Verify authorization error message | bookId: valid_book_id, quantity: 1, no token | HTTP 401, success: false, message: "No autorizado: se requiere un token de autenticación" | |
| TC-CART-005 | Cart | Reject adding with invalid token | High | Invalid authentication token provided | 1. Send POST /carts/add with invalid token<br>2. Verify HTTP 401 response<br>3. Verify invalid token error message | bookId: valid_book_id, quantity: 1, token: "invalid-token" | HTTP 401, success: false, message: "No autorizado: token inválido o expirado" | |
| TC-CART-006 | Cart | Reject adding if bookId is missing | High | User authenticated with valid token | 1. Send POST /carts/add without bookId<br>2. Verify HTTP 400 response<br>3. Verify validation error message | quantity: 1 (no bookId) | HTTP 400, success: false, message: "Error de validación", errors contain "bookId es requerido" | |
| TC-CART-007 | Cart | Reject adding if bookId is invalid UUID | High | User authenticated with valid token | 1. Send POST /carts/add with invalid UUID format<br>2. Verify HTTP 400 response<br>3. Verify UUID validation error | bookId: "invalid-uuid", quantity: 1 | HTTP 400, success: false, message: "Error de validación", errors contain "bookId debe ser un UUID válido" | |
| TC-CART-008 | Cart | Reject adding if book does not exist | High | User authenticated with valid token, bookId does not exist in database | 1. Send POST /carts/add with non-existent bookId<br>2. Verify HTTP 404 response<br>3. Verify book not found message | bookId: non_existent_uuid, quantity: 1 | HTTP 404, success: false, message: "Libro no encontrado" | |
| TC-CART-009 | Cart | Reject adding if quantity is less than 1 | High | User authenticated with valid token, book exists | 1. Send POST /carts/add with quantity 0<br>2. Verify HTTP 400 response<br>3. Verify quantity validation error | bookId: valid_book_id, quantity: 0 | HTTP 400, success: false, message: "Error de validación", errors contain "La cantidad debe ser un número entero positivo" | |
| TC-CART-010 | Cart | Reject adding if quantity is not integer | High | User authenticated with valid token, book exists | 1. Send POST /carts/add with decimal quantity<br>2. Verify HTTP 400 response<br>3. Verify integer validation error | bookId: valid_book_id, quantity: 1.5 | HTTP 400, success: false, message: "Error de validación", errors contain "La cantidad debe ser un número entero positivo" | |
| TC-CART-011 | Cart | Reject adding if quantity exceeds available stock | High | User authenticated with valid token, book exists with limited stock | 1. Send POST /carts/add with quantity greater than stock<br>2. Verify HTTP 409 response<br>3. Verify insufficient stock message | bookId: valid_book_id (stock: 10), quantity: 65 | HTTP 409, success: false, message: "Stock insuficiente para el libro solicitado" | |
| TC-CART-012 | Cart | Reject adding if user has pending order | High | User authenticated, user has PENDING order in progress | 1. Create PENDING order for user<br>2. Attempt to add book to cart<br>3. Verify HTTP 409 response<br>4. Verify pending order error message | bookId: valid_book_id, quantity: 1, user with PENDING order | HTTP 409, success: false, message: "Ya tienes una orden pendiente en proceso", response contains pending order details | |
| TC-CART-013 | Cart | Get empty cart for user without items | Medium | User authenticated with valid token, cart is empty | 1. Send GET /carts/ with valid token<br>2. Verify HTTP 200 response<br>3. Verify cart is empty with correct structure | Valid token | HTTP 200, success: true, data.items: [], data.totalPrice: 0, data.totalItems: 0 | |
| TC-CART-014 | Cart | Get cart with multiple items | High | User authenticated, multiple books added to cart | 1. Add 3 different books to cart with various quantities<br>2. Send GET /carts/ with valid token<br>3. Verify all items are listed correctly<br>4. Verify totals are calculated correctly | Multiple books with different quantities | HTTP 200, success: true, data.items.length: 3, correct totalItems and totalPrice | |
| TC-CART-015 | Cart | Calculate cart totals correctly | High | User authenticated, multiple items in cart with different prices | 1. Add books with known prices and quantities<br>2. Send GET /carts/ with valid token<br>3. Verify totalItems is sum of quantities<br>4. Verify totalPrice is sum of (price * quantity) | Book A: price 29.99, quantity 2 (59.98)<br>Book B: price 15.0, quantity 1 (15.0)<br>Book C: price 50.0, quantity 5 (250.0) | HTTP 200, success: true, totalItems: 8, totalPrice: 324.98 | |
| TC-CART-016 | Cart | Include pending order information if exists | Medium | User authenticated, cart has items, user has PENDING order | 1. Add book to cart<br>2. Create PENDING order for user<br>3. Send GET /carts/ with valid token<br>4. Verify pendingOrder section is included | Valid token, user with PENDING order | HTTP 200, success: true, data.pendingOrder exists with id, total, createdAt, expiresAt | |
| TC-CART-017 | Cart | Reject getting cart without authentication | High | No authentication token provided | 1. Send GET /carts/ without token<br>2. Verify HTTP 401 response<br>3. Verify authorization error message | No token | HTTP 401, success: false, message: "No autorizado: se requiere un token de autenticación" | |
| TC-CART-018 | Cart | Update cart item quantity successfully | High | User authenticated, item exists in cart | 1. Add book to cart with quantity 1<br>2. Send PUT /carts/:cartId with new quantity<br>3. Verify HTTP 200 response<br>4. Verify quantity is updated | cartId: valid_cart_id, quantity: 3 | HTTP 200, success: true, message: "Item del carrito actualizado exitosamente", updated quantity: 3 | |
| TC-CART-019 | Cart | Increment cart item quantity | Medium | User authenticated, item exists in cart | 1. Add book to cart with quantity 1<br>2. Update to quantity 5<br>3. Verify quantity increased correctly | cartId: valid_cart_id, quantity: 5 | HTTP 200, success: true, quantity: 5 | |
| TC-CART-020 | Cart | Decrement cart item quantity | Medium | User authenticated, item exists in cart with higher quantity | 1. Add book to cart with quantity 5<br>2. Update to quantity 2<br>3. Verify quantity decreased correctly | cartId: valid_cart_id, quantity: 2 | HTTP 200, success: true, quantity: 2 | |
| TC-CART-021 | Cart | Reject update if cartId is invalid UUID | High | User authenticated with valid token | 1. Send PUT /carts/:cartId with invalid UUID<br>2. Verify HTTP 400 response<br>3. Verify UUID validation error | cartId: "invalid-uuid", quantity: 5 | HTTP 400, success: false, message: "Error de validación", errors contain "cartId debe ser un UUID válido" | |
| TC-CART-022 | Cart | Reject update if item does not exist | High | User authenticated, cartId does not exist in database | 1. Send PUT /carts/:cartId with non-existent cartId<br>2. Verify HTTP 404 response<br>3. Verify item not found message | cartId: non_existent_uuid, quantity: 5 | HTTP 404, success: false, message: "Item del carrito no encontrado" | |
| TC-CART-023 | Cart | Reject update if item belongs to another user | High | User authenticated, cartId belongs to different user | 1. User A adds book to cart<br>2. User B attempts to update User A's cart item<br>3. Verify HTTP 404 response<br>4. Verify item not found message (security by obscurity) | User B token, User A's cartId, quantity: 10 | HTTP 404, success: false, message: "Item del carrito no encontrado" | |
| TC-CART-024 | Cart | Reject update if new quantity exceeds stock | High | User authenticated, item exists, stock is limited | 1. Add book to cart with quantity 1<br>2. Update to quantity greater than available stock<br>3. Verify HTTP 409 response<br>4. Verify insufficient stock message | cartId: valid_cart_id, quantity: 11 (stock: 10) | HTTP 409, success: false, message: "Stock insuficiente para el libro solicitado" | |
| TC-CART-025 | Cart | Reject update if quantity is 0 or negative | High | User authenticated, item exists in cart | 1. Attempt to update quantity to 0<br>2. Attempt to update quantity to negative number<br>3. Verify HTTP 400 response for both<br>4. Verify quantity validation error | cartId: valid_cart_id, quantity: 0 or -5 | HTTP 400, success: false, message: "Error de validación", errors contain "La cantidad debe ser un número entero positivo" | |
| TC-CART-026 | Cart | Reject update if user has pending order | High | User authenticated, user has PENDING order in progress | 1. Add book to cart<br>2. Create PENDING order for user<br>3. Attempt to update cart item quantity<br>4. Verify HTTP 409 response<br>5. Verify pending order error message | cartId: valid_cart_id, quantity: 5, user with PENDING order | HTTP 409, success: false, message: "Ya tienes una orden pendiente en proceso" | |
| TC-CART-027 | Cart | Remove cart item successfully | High | User authenticated, item exists in cart | 1. Add book to cart<br>2. Send DELETE /carts/:cartId with valid token<br>3. Verify HTTP 200 response<br>4. Verify item is removed | cartId: valid_cart_id | HTTP 200, success: true, message: "Libro eliminado del carrito exitosamente", data contains deleted cartId | |
| TC-CART-028 | Cart | Reject removal if item does not exist | High | User authenticated, cartId does not exist | 1. Send DELETE /carts/:cartId with non-existent cartId<br>2. Verify HTTP 404 response<br>3. Verify item not found message | cartId: non_existent_uuid | HTTP 404, success: false, message: "Item del carrito no encontrado" | |
| TC-CART-029 | Cart | Reject removal if item belongs to another user | High | User authenticated, cartId belongs to different user | 1. User A adds book to cart<br>2. User B attempts to remove User A's cart item<br>3. Verify HTTP 404 response<br>4. Verify item not found message (security by obscurity) | User B token, User A's cartId | HTTP 404, success: false, message: "Item del carrito no encontrado" | |
| TC-CART-030 | Cart | Reject removal if user has pending order | High | User authenticated, user has PENDING order in progress | 1. Add book to cart<br>2. Create PENDING order for user<br>3. Attempt to remove cart item<br>4. Verify HTTP 409 response<br>5. Verify pending order error message | cartId: valid_cart_id, user with PENDING order | HTTP 409, success: false, message: "Ya tienes una orden pendiente en proceso" | |
| TC-CART-031 | Cart | Clear all cart items successfully | High | User authenticated, multiple items in cart | 1. Add multiple books to cart<br>2. Send DELETE /carts/ with valid token<br>3. Verify HTTP 200 response<br>4. Verify all items are removed<br>5. Verify count of deleted items | Valid token, cart with 3 items | HTTP 200, success: true, message: "Carrito vaciado exitosamente", data.count: 3 | |
| TC-CART-032 | Cart | Clear cart when already empty | Medium | User authenticated, cart is empty | 1. Send DELETE /carts/ with valid token<br>2. Verify HTTP 200 response<br>3. Verify success with count 0 | Valid token, empty cart | HTTP 200, success: true, message: "Carrito vaciado exitosamente", data.count: 0 | |
| TC-CART-033 | Cart | Reject clearing if user has pending order | High | User authenticated, user has PENDING order in progress | 1. Add book to cart<br>2. Create PENDING order for user<br>3. Attempt to clear cart<br>4. Verify HTTP 409 response<br>5. Verify pending order error message | Valid token, user with PENDING order | HTTP 409, success: false, message: "Ya tienes una orden pendiente en proceso" | |
| TC-CART-034 | Cart | Cart isolation between different users | High | Two different users authenticated | 1. User A adds book to cart<br>2. User B checks their cart<br>3. Verify User B's cart is empty<br>4. Verify carts are completely isolated | User A adds book, User B checks cart | HTTP 200 for User B, User B's cart is empty (items: [], totalPrice: 0) | |
| TC-CART-035 | Cart | Dynamic stock validation on update | Medium | User authenticated, item in cart, stock changes externally | 1. Add book to cart with quantity 5<br>2. Reduce book stock in database to 2<br>3. Attempt to update quantity to 3<br>4. Verify HTTP 409 response<br>5. Verify insufficient stock message | cartId: valid_cart_id, quantity: 3, external stock reduction to 2 | HTTP 409, success: false, message: "Stock insuficiente para el libro solicitado" | |
| TC-CART-036 | Cart | Handle price changes in catalog correctly | Medium | User authenticated, item in cart, book price changes | 1. Add book to cart with original price<br>2. Update book price in catalog<br>3. Get cart and verify new price is reflected | Original price: 29.99, updated price: 99.99 | HTTP 200, cart shows updated price: 99.99, totalPrice reflects new price | |
| TC-CART-037 | Cart | Cart persistence after logout and new login | Medium | User authenticated, items in cart | 1. Add books to cart<br>2. User logs out and logs in again<br>3. Get cart with new token<br>4. Verify cart items persist | Multiple books in cart, new login token | HTTP 200, cart contains same items with same quantities as before logout | |
| TC-CART-038 | Cart | Allow re-adding book after clearing cart | Medium | User authenticated, cart was cleared | 1. Add book to cart with quantity 3<br>2. Clear cart completely<br>3. Add same book again with quantity 1<br>4. Verify book is added successfully | Same book added, cleared, then added again | HTTP 200, success: true, book added with quantity 1 | |
| TC-CART-039 | Cart | Cart items ordered by creation date | Low | User authenticated, multiple items added at different times | 1. Add Book A, then Book B, then Book C<br>2. Get cart<br>3. Verify items are ordered by creation date (newest first) | Book A (first), Book B (second), Book C (third) | HTTP 200, items ordered: Book C, Book B, Book A (newest first) | |
