# Manual Test Cases - Favorites Module

| ID | Module | Title | Priority | Preconditions | Steps | Test Data | Expected Result | Status |
| -- | ------ | ------ | --------- | -------------- | ----- | --------------- | ------------------ | ------ |
| TC-FAV-001 | Favorites | Add book to favorites successfully | High | User authenticated with valid token, book exists in database | 1. Send POST /users/:userId/favorites/:bookId with valid token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify isFavorite is true<br>5. Verify success message | Valid token, userId: authenticated_user_id, bookId: valid_book_id | HTTP 200, success: true, message: "Libro agregado a favoritos", isFavorite: true | |
| TC-FAV-002 | Favorites | Remove book from favorites successfully | High | User authenticated, book is already in user's favorites | 1. Send POST /users/:userId/favorites/:bookId with valid token (book already favorited)<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify isFavorite is false<br>5. Verify removal message | Valid token, userId: authenticated_user_id, bookId: already_favorited_book_id | HTTP 200, success: true, message: "Libro eliminado de favoritos", isFavorite: false | |
| TC-FAV-003 | Favorites | Reject adding without authentication | High | No authentication token provided | 1. Send POST /users/:userId/favorites/:bookId without token<br>2. Verify HTTP 401 response<br>3. Verify authorization error message | userId: valid_user_id, bookId: valid_book_id, no token | HTTP 401, success: false, message: "No autorizado: se requiere un token de autenticación" | |
| TC-FAV-004 | Favorites | Reject modifying another user's favorites | High | User authenticated, attempting to modify different user's favorites | 1. Send POST /users/:anotherUserId/favorites/:bookId with authenticated user's token<br>2. Verify HTTP 403 response<br>3. Verify forbidden error message | Authenticated token, userId: different_user_id, bookId: valid_book_id | HTTP 403, success: false, message: "Prohibido: Solo puedes modificar tus propios favoritos" | |
| TC-FAV-005 | Favorites | Reject if userId does not match authenticated user | High | User authenticated, userId in URL does not match authenticated user | 1. Send POST /users/:nonExistentUserId/favorites/:bookId with valid token<br>2. Verify HTTP 403 response<br>3. Verify forbidden error message (checks userId match before existence) | Valid token, userId: "non-existent-id", bookId: valid_book_id | HTTP 403, success: false, message: "Prohibido: Solo puedes modificar tus propios favoritos" | |
| TC-FAV-006 | Favorites | Get empty favorites list | Medium | User authenticated with valid token, no favorites exist | 1. Send GET /users/:userId/favorites with valid token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify favorites array is empty | Valid token, userId: authenticated_user_id | HTTP 200, success: true, data: [] | |
| TC-FAV-007 | Favorites | Get favorites list with books | High | User authenticated, multiple books in favorites | 1. Add multiple books to favorites<br>2. Send GET /users/:userId/favorites with valid token<br>3. Verify HTTP 200 response<br>4. Verify success is true<br>5. Verify all favorited books are returned<br>6. Verify each book has correct structure | Valid token, userId: authenticated_user_id, multiple favorited books | HTTP 200, success: true, data array contains all favorited books with correct book structure | |
| TC-FAV-008 | Favorites | Reject getting favorites without authentication | High | No authentication token provided | 1. Send GET /users/:userId/favorites without token<br>2. Verify HTTP 401 response<br>3. Verify authorization error message | userId: valid_user_id, no token | HTTP 401, success: false, message: "No autorizado: se requiere un token de autenticación" | |
| TC-FAV-009 | Favorites | Reject viewing another user's favorites | High | User authenticated, attempting to view different user's favorites | 1. Send GET /users/:anotherUserId/favorites with authenticated user's token<br>2. Verify HTTP 403 response<br>3. Verify forbidden error message | Authenticated token, userId: different_user_id | HTTP 403, success: false, message: "Prohibido: Solo puedes ver tus propios favoritos" | |
| TC-FAV-010 | Favorites | Reject viewing if userId does not match authenticated user | High | User authenticated, userId in URL does not match authenticated user | 1. Send GET /users/:nonExistentUserId/favorites with valid token<br>2. Verify HTTP 403 response<br>3. Verify forbidden error message (checks userId match before existence) | Valid token, userId: "non-existent-id" | HTTP 403, success: false, message: "Prohibido: Solo puedes ver tus propios favoritos" | |
| TC-FAV-011 | Favorites | Correctly reflect favorites after adding and removing | Medium | User authenticated, books exist in database | 1. Get favorites (expect empty)<br>2. Add first book to favorites<br>3. Get favorites (expect 1 book)<br>4. Add second book to favorites<br>5. Get favorites (expect 2 books)<br>6. Remove first book from favorites<br>7. Get favorites (expect 1 book, second only)<br>8. Remove second book from favorites<br>9. Get favorites (expect empty) | Valid token, userId: authenticated_user_id, bookId1: valid_book_id, bookId2: valid_book_id2 | HTTP 200 for all operations, favorites list correctly reflects current state after each operation | |
