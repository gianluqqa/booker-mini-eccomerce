# Manual Test Cases - Checkout Module

| ID | Module | Title | Priority | Preconditions | Steps | Test Data | Expected Result | Status |
| -- | ------ | ------ | --------- | -------------- | ----- | --------------- | ------------------ | ------ |
| TC-CHECKOUT-001 | Stock Reservation | Create stock reservation successfully | High | User authenticated, cart has items with available stock | 1. Add books to cart<br>2. Send POST /checkout/reserve with valid token<br>3. Verify HTTP 201 response<br>4. Verify reservation is created with correct items and total | Valid token, cart with items | HTTP 201, success: true, message: "Reserva de stock creada exitosamente", reservation with items, totalAmount, expiresAt | |
| TC-CHECKOUT-002 | Stock Reservation | Reject reservation without authentication | High | No authentication token provided | 1. Send POST /checkout/reserve without token<br>2. Verify HTTP 401 response<br>3. Verify authorization error message | No token | HTTP 401, success: false, message: "No autorizado: se requiere un token de autenticación" | |
| TC-CHECKOUT-003 | Stock Reservation | Reject reservation if pending order exists | High | User authenticated, user has PENDING order in progress | 1. Create PENDING order for user<br>2. Attempt to create stock reservation<br>3. Verify HTTP 409 response<br>4. Verify pending order error message with order details | Valid token, user with PENDING order | HTTP 409, success: false, message: "Ya tienes una orden pendiente en proceso", response contains pending order id | |
| TC-CHECKOUT-004 | Stock Reservation | Reject reservation if cart is empty | High | User authenticated, cart is empty | 1. Send POST /checkout/reserve with valid token<br>2. Verify HTTP 400 response<br>3. Verify empty cart error message | Valid token, empty cart | HTTP 400, success: false, message: "Tu carrito está vacío. Agrega libros antes de iniciar el checkout." | |
| TC-CHECKOUT-005 | Stock Reservation | Reject reservation if insufficient stock | High | User authenticated, cart has items, stock reduced externally | 1. Add book to cart with quantity 5<br>2. Reduce book stock in database to 2<br>3. Attempt to create stock reservation<br>4. Verify HTTP 409 response<br>5. Verify insufficient stock message | Valid token, book stock reduced to 2, cart quantity: 5 | HTTP 409, success: false, message: "Stock insuficiente para el libro solicitado" | |
| TC-CHECKOUT-006 | Stock Reservation | Return existing reservation if already active (Idempotency) | Medium | User authenticated, active reservation exists | 1. Create initial stock reservation<br>2. Add different book to cart<br>3. Attempt to create new reservation without canceling<br>4. Verify same reservation is returned | Valid token, existing reservation, cart changes | HTTP 201, success: true, same reservation id returned, items from original reservation (not cart changes) | |
| TC-CHECKOUT-007 | Checkout Processing | Create pending order without payment | High | User authenticated, stock reserved, cart has items | 1. Add books to cart<br>2. Create stock reservation<br>3. Send POST /checkout/confirm without payment data<br>4. Verify HTTP 201 response<br>5. Verify order is created in PENDING status with expiration | Valid token, active reservation | HTTP 201, success: true, message: "Orden pendiente creada exitosamente", order.status: "pending", order.expiresAt present | |
| TC-CHECKOUT-008 | Checkout Processing | Process payment successfully | High | User authenticated, PENDING order exists, valid payment data | 1. Create PENDING order<br>2. Send POST /checkout/pay with valid payment data<br>3. Verify HTTP 200 response<br>4. Verify order status changes to PAID<br>5. Verify stock is reduced | Valid token, PENDING order, valid payment data | HTTP 200, success: true, message: "Pago procesado exitosamente", order.status: "paid", stock reduced | |
| TC-CHECKOUT-009 | Checkout Processing | Reject payment with invalid data | High | User authenticated, PENDING order exists, invalid payment format | 1. Create PENDING order<br>2. Send POST /checkout/pay with invalid card number format<br>3. Verify HTTP 400 response<br>4. Verify invalid payment data error message | Valid token, PENDING order, cardNumber: "123-456-789-235-" | HTTP 400, success: false, message: "Datos de pago inválidos" | |
| TC-CHECKOUT-010 | Checkout Processing | Reject checkout without active reservation | High | User authenticated, cart has items, no reservation | 1. Add books to cart<br>2. Send POST /checkout/confirm without reservation<br>3. Verify HTTP 400 response<br>4. Verify no active reservation error message | Valid token, cart with items, no reservation | HTTP 400, success: false, message: "No tienes una reserva de stock activa. Por favor, inicia el proceso de nuevo." | |
| TC-CHECKOUT-011 | Checkout Processing | Reject payment if no pending order exists | High | User authenticated, no PENDING order | 1. Send POST /checkout/pay with valid payment data<br>2. Verify HTTP 404 response<br>3. Verify order not found error message | Valid token, no PENDING order, valid payment data | HTTP 404, success: false, message: "No se encontró ninguna orden pendiente para procesar el pago." | |
| TC-CHECKOUT-012 | Checkout Cancellation | Cancel checkout successfully (reservation and order) | High | User authenticated, active reservation and PENDING order exist | 1. Create stock reservation<br>2. Create PENDING order<br>3. Send DELETE /checkout/cancel with valid token<br>4. Verify HTTP 200 response<br>5. Verify reservation and order are deleted<br>6. Verify stock is restored | Valid token, active reservation, PENDING order | HTTP 200, success: true, message contains "cancelada", reservation and order deleted from database, stock restored | |
| TC-CHECKOUT-013 | Checkout Cancellation | Reject cancellation if nothing to cancel | Medium | User authenticated, no active reservation or PENDING order | 1. Send DELETE /checkout/cancel with valid token<br>2. Verify HTTP 404 response<br>3. Verify nothing to cancel error message | Valid token, no active reservation or PENDING order | HTTP 404, success: false, message: "No tienes ninguna reserva o pedido pendiente para cancelar." | |
| TC-CHECKOUT-014 | Integration | Handle expired reservation | High | User authenticated, reservation exists but has expired | 1. Create stock reservation<br>2. Modify reservation expiresAt to past date<br>3. Attempt to process checkout<br>4. Verify HTTP 410 response<br>5. Verify reservation expired error message | Valid token, expired reservation | HTTP 410, success: false, message: "Tu reserva de stock ha expirado. Por favor, inicia el proceso de nuevo." | |
| TC-CHECKOUT-015 | Integration | Handle reservation concurrency (insufficient final stock) | High | User authenticated, PENDING order exists, stock depleted externally | 1. Create PENDING order with quantity 8 (stock: 10)<br>2. Reduce book stock to 0 externally<br>3. Attempt to pay for order<br>4. Verify HTTP 409 response<br>5. Verify insufficient final stock error message | Valid token, PENDING order, stock reduced to 0 | HTTP 409, success: false, message: "No hay stock suficiente para completar el pago de esta orden." | |
| TC-CHECKOUT-016 | Integration | Calculate total correctly with multiple items | Medium | User authenticated, cart has multiple items with different prices | 1. Add Book A (29.99, quantity 2) and Book B (19.99, quantity 1)<br>2. Create stock reservation<br>3. Verify total amount is calculated correctly | Book A: 29.99 * 2 = 59.98, Book B: 19.99 * 1 = 19.99 | HTTP 201, totalAmount: 79.97 | |
| TC-CHECKOUT-017 | Integration | Update stock after successful payment | High | User authenticated, items in cart, payment processed | 1. Add book with quantity 3 (stock: 10)<br>2. Create reservation and PENDING order<br>3. Process payment successfully<br>4. Verify book stock is reduced to 7 | Book stock: 10, quantity: 3 | HTTP 200, payment successful, updated stock: 7 | |
| TC-CHECKOUT-018 | Integration | Handle expired order during payment | High | User authenticated, PENDING order exists but has expired | 1. Create PENDING order<br>2. Modify order expiresAt to past date<br>3. Attempt to pay for order<br>4. Verify HTTP 410 response<br>5. Verify order expired error message | Valid token, expired PENDING order | HTTP 410, success: false, message: "La orden ha expirado. Por favor, inicia el proceso de nuevo." | |
| TC-CHECKOUT-019 | Integration | Return same order if already paid (Idempotency) | Medium | User authenticated, order already in PAID status | 1. Create PENDING order<br>2. Process payment successfully<br>3. Attempt to pay again for same order<br>4. Verify same PAID order is returned | Valid token, PAID order, payment data | HTTP 200, success: true, returns same PAID order without processing payment again | |
| TC-CHECKOUT-020 | Integration | Reject payment with incomplete payment data | High | User authenticated, PENDING order exists, incomplete payment data | 1. Create PENDING order<br>2. Send POST /checkout/pay with missing cardName<br>3. Verify HTTP 400 response<br>4. Verify incomplete payment data error message | Valid token, PENDING order, missing cardName | HTTP 400, success: false, message: "Datos de pago incompletos para procesar la orden pendiente" |

---

## Execution History

| Test Case ID | Date | Result | Notes |
|---------------|------|--------|-------|
| TC-CHECKOUT-001 | 2024-04-15 | ❌ Fail | Bug reported as BUG-CHECKOUT-001-stock-reservation. |
| TC-CHECKOUT-001 | 2024-04-17 | ✅ Pass | Bug fixed and verified successfully. |
| TC-CHECKOUT-008 | 2024-04-20 | ❌ Fail | Bug reported as BUG-CHECKOUT-002-process-payment. |
| TC-CHECKOUT-008 | 2024-04-22 | ✅ Pass | Bug fixed and verified successfully. |
| TC-CHECKOUT-012 | 2024-04-25 | ❌ Fail | Bug reported as BUG-CHECKOUT-003-cancel-checkout. |
| TC-CHECKOUT-012 | 2024-04-27 | ✅ Pass | Bug fixed and verified successfully. |
| TC-CHECKOUT-014 | 2024-04-30 | ❌ Fail | Bug reported as BUG-CHECKOUT-004-expired-reservation. |
| TC-CHECKOUT-014 | 2024-05-02 | ✅ Pass | Bug fixed and verified successfully. |
| TC-CHECKOUT-015 | 2024-05-05 | ❌ Fail | Bug reported as BUG-CHECKOUT-005-stock-concurrency. |
| TC-CHECKOUT-015 | 2024-05-07 | ✅ Pass | Bug fixed and verified successfully. | |
