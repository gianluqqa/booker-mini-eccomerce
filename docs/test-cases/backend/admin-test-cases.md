# Manual Test Cases - Admin Module

| ID | Module | Title | Priority | Preconditions | Steps | Test Data | Expected Result | Status |
| -- | ------ | ------ | --------- | -------------- | ----- | --------------- | ------------------ | ------ |
| TC-ADMIN-001 | Books | Create book as administrator | High | Admin user authenticated with valid token | 1. Send POST /books/ with admin token and valid book data<br>2. Verify HTTP 201 response<br>3. Verify success is true<br>4. Verify book is created with provided data | title: "Admin Created Book", author: "Admin Author", price: 15.99, stock: 50, genre: "TestGenre", description: "A test book created by admin" | HTTP 201, success: true, message: "Libro creado exitosamente", book with provided data | |
| TC-ADMIN-002 | Books | Reject book creation by normal user | High | Normal user authenticated with valid token | 1. Send POST /books/ with normal user token and book data<br>2. Verify HTTP 403 response<br>3. Verify forbidden message | Any book data with normal user token | HTTP 403, success: false, message: "Prohibido: se requiere rol de administrador" | |
| TC-ADMIN-003 | Books | Update book as administrator | High | Admin user authenticated, book exists in database | 1. Send PUT /books/:id with admin token and update data<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify book is updated with new data | bookId: existing_book_id, price: 20.99 | HTTP 200, success: true, message: "Libro actualizado exitosamente", updated price: 20.99 | |
| TC-ADMIN-004 | Books | Reject book update by normal user | High | Normal user authenticated, book exists in database | 1. Send PUT /books/:id with normal user token and update data<br>2. Verify HTTP 403 response<br>3. Verify forbidden message | bookId: existing_book_id, price: 20 | HTTP 403, success: false, message: "Prohibido: se requiere rol de administrador" | |
| TC-ADMIN-005 | Books | Delete book as administrator | High | Admin user authenticated, book exists in database | 1. Send DELETE /books/:id with admin token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify book is deleted | bookId: existing_book_id | HTTP 200, success: true, message: "Libro eliminado exitosamente", data contains deleted book id | |
| TC-ADMIN-006 | Books | Reject book deletion by normal user | High | Normal user authenticated, book exists in database | 1. Send DELETE /books/:id with normal user token<br>2. Verify HTTP 403 response<br>3. Verify forbidden message | bookId: existing_book_id | HTTP 403, success: false, message: "Prohibido: Solo los administradores pueden eliminar libros" | |
| TC-ADMIN-007 | Orders | View all orders from all users as administrator | High | Admin user authenticated, multiple orders exist from different users | 1. Send GET /orders/admin/all with admin token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify response contains orders from multiple users | Admin token | HTTP 200, success: true, data array contains orders from all users | |
| TC-ADMIN-008 | Orders | Reject viewing all orders by normal user | High | Normal user authenticated | 1. Send GET /orders/admin/all with normal user token<br>2. Verify HTTP 403 response<br>3. Verify forbidden message | Normal user token | HTTP 403, success: false, message: "Prohibido: se requiere rol de administrador" | |
| TC-ADMIN-009 | Orders | Cancel PAID order and restore stock as administrator | High | Admin user authenticated, order exists in PAID status with items | 1. Send POST /orders/admin/cancel/:id with admin token<br>2. Verify HTTP 200 response<br>3. Verify order status changes to CANCELLED<br>4. Verify stock is restored for order items | orderId: existing_paid_order_id | HTTP 200, success: true, order.status: "CANCELLED", stock restored for books | |
| TC-ADMIN-010 | Orders | Cancel PENDING order as administrator | High | Admin user authenticated, order exists in PENDING status with items | 1. Send POST /orders/admin/cancel/:id with admin token<br>2. Verify HTTP 200 response<br>3. Verify order status changes to CANCELLED<br>4. Verify stock is restored for order items | orderId: existing_pending_order_id | HTTP 200, success: true, order.status: "CANCELLED", stock restored for books | |
| TC-ADMIN-011 | Orders | Reject cancelling already CANCELLED order | Medium | Admin user authenticated, order exists in CANCELLED status | 1. Send POST /orders/admin/cancel/:id with admin token for CANCELLED order<br>2. Verify HTTP 400 response<br>3. Verify validation error message | orderId: existing_cancelled_order_id | HTTP 400, success: false, message: "Solo se pueden cancelar órdenes en estado PAID o PENDING" | |
| TC-ADMIN-012 | Orders | Reject order cancellation by normal user | High | Normal user authenticated, order exists in PAID status | 1. Send POST /orders/admin/cancel/:id with normal user token<br>2. Verify HTTP 403 response<br>3. Verify forbidden message | orderId: existing_order_id, normal user token | HTTP 403, success: false, message: "Prohibido: se requiere rol de administrador" | |
| TC-ADMIN-013 | Orders | Clear all orders as administrator | High | Admin user authenticated, multiple orders exist in database | 1. Send DELETE /orders/admin/clear-all with admin token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify all orders are deleted | Admin token | HTTP 200, success: true, message: "Todas las órdenes han sido eliminadas exitosamente" | |
| TC-ADMIN-014 | Orders | Reject clearing all orders by normal user | High | Normal user authenticated | 1. Send DELETE /orders/admin/clear-all with normal user token<br>2. Verify HTTP 403 response<br>3. Verify forbidden message | Normal user token | HTTP 403, success: false, message: "Prohibido: se requiere rol de administrador" | |
| TC-ADMIN-015 | Orders | Clear all cancelled orders as administrator | Medium | Admin user authenticated, cancelled orders exist in database | 1. Send DELETE /orders/admin/clear-cancelled with admin token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify only cancelled orders are deleted | Admin token | HTTP 200, success: true, message: "Todas las órdenes canceladas han sido eliminadas exitosamente" | |
| TC-ADMIN-016 | Orders | Reject clearing cancelled orders by normal user | High | Normal user authenticated | 1. Send DELETE /orders/admin/clear-cancelled with normal user token<br>2. Verify HTTP 403 response<br>3. Verify forbidden message | Normal user token | HTTP 403, success: false, message: "Prohibido: se requiere rol de administrador" | |
| TC-ADMIN-017 | Reviews | View all reviews with metadata as administrator | High | Admin user authenticated, reviews exist in database | 1. Send GET /reviews/admin/all with admin token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify response contains reviews array and metadata | Admin token | HTTP 200, success: true, data is array, meta contains total, page, limit | |
| TC-ADMIN-018 | Reviews | Reject viewing all reviews by normal user | High | Normal user authenticated | 1. Send GET /reviews/admin/all with normal user token<br>2. Verify HTTP 403 response<br>3. Verify forbidden message | Normal user token | HTTP 403, success: false, message: "Prohibido: se requiere rol de administrador" | |
| TC-ADMIN-019 | Reviews | Filter reviews by book title as administrator | Medium | Admin user authenticated, reviews exist for specific books | 1. Send GET /reviews/admin/all?book=BookTitle with admin token<br>2. Verify HTTP 200 response<br>3. Verify filtered results contain only reviews for specified book | Admin token, book: "Admin Review" | HTTP 200, success: true, data array contains reviews with book.title containing "Admin Review" | |
| TC-ADMIN-020 | Reviews | Delete any review as administrator | High | Admin user authenticated, review exists in database | 1. Send DELETE /reviews/admin/:reviewId with admin token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify review is deleted from database | Admin token, reviewId: existing_review_id | HTTP 200, success: true, message: "Reseña eliminada exitosamente", review no longer exists in database | |
| TC-ADMIN-021 | Reviews | Reject review deletion by normal user | High | Normal user authenticated, review exists in database | 1. Send DELETE /reviews/admin/:reviewId with normal user token<br>2. Verify HTTP 403 response<br>3. Verify forbidden message | Normal user token, reviewId: existing_review_id | HTTP 403, success: false, message: "Prohibido: se requiere rol de administrador" | |
| TC-ADMIN-022 | Users | View all users as administrator | High | Admin user authenticated, multiple users exist in database | 1. Send GET /users/ with admin token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify response contains users array | Admin token | HTTP 200, success: true, data is array of users | |
| TC-ADMIN-023 | Users | Reject viewing all users by normal user | High | Normal user authenticated | 1. Send GET /users/ with normal user token<br>2. Verify HTTP 403 response<br>3. Verify forbidden message | Normal user token | HTTP 403, success: false, message: "Prohibido: se requiere rol de administrador" | |
| TC-ADMIN-024 | Users | View user by ID as administrator | High | Admin user authenticated, user exists in database | 1. Send GET /users/:userId with admin token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify response contains requested user data | Admin token, userId: existing_user_id | HTTP 200, success: true, data.id matches requested userId | |
| TC-ADMIN-025 | Users | Reject viewing user by ID by normal user | High | Normal user authenticated | 1. Send GET /users/:userId with normal user token<br>2. Verify HTTP 403 response<br>3. Verify forbidden message | Normal user token, userId: existing_user_id | HTTP 403, success: false, message: "Prohibido: se requiere rol de administrador" | |
| TC-ADMIN-026 | Users | Delete user as administrator | High | Admin user authenticated, non-admin user exists in database | 1. Send DELETE /users/:userId with admin token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify user is deleted from database | Admin token, userId: existing_non_admin_user_id | HTTP 200, success: true, message: "Usuario eliminado exitosamente", user no longer exists in database | |
| TC-ADMIN-027 | Users | Reject user deletion by normal user | High | Normal user authenticated | 1. Send DELETE /users/:userId with normal user token<br>2. Verify HTTP 403 response<br>3. Verify forbidden message | Normal user token, userId: existing_user_id | HTTP 403, success: false, message: "Prohibido: Solo los administradores pueden eliminar usuarios" | |
| TC-ADMIN-028 | Users | Delete all non-admin users as administrator | High | Admin user authenticated, multiple non-admin users exist | 1. Send DELETE /users/ with admin token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify all non-admin users are deleted, admin remains | Admin token | HTTP 200, success: true, message: "Se eliminaron todos los usuarios clientes exitosamente", data contains deletedCount, admin user remains | |
| TC-ADMIN-029 | Users | Reject deleting all users by normal user | High | Normal user authenticated | 1. Send DELETE /users/ with normal user token<br>2. Verify HTTP 403 response<br>3. Verify forbidden message | Normal user token | HTTP 403, success: false, message: "Prohibido: Solo los administradores pueden realizar esta acción" |

---

## Execution History

| Test Case ID | Date | Result | Notes |
|---------------|------|--------|-------|
| TC-ADMIN-001 | 2024-02-05 | ❌ Fail | Bug reported as BUG-ADMIN-001-create-book. |
| TC-ADMIN-001 | 2024-02-07 | ✅ Pass | Bug fixed and verified successfully. |
| TC-ADMIN-009 | 2024-02-10 | ❌ Fail | Bug reported as BUG-ADMIN-002-cancel-order. |
| TC-ADMIN-009 | 2024-02-12 | ✅ Pass | Bug fixed and verified successfully. |
| TC-ADMIN-013 | 2024-02-15 | ❌ Fail | Bug reported as BUG-ADMIN-003-clear-all-orders. |
| TC-ADMIN-013 | 2024-02-17 | ✅ Pass | Bug fixed and verified successfully. |
| TC-ADMIN-020 | 2024-02-20 | ❌ Fail | Bug reported as BUG-ADMIN-004-delete-review. |
| TC-ADMIN-020 | 2024-02-22 | ✅ Pass | Bug fixed and verified successfully. |
| TC-ADMIN-026 | 2024-02-25 | ❌ Fail | Bug reported as BUG-ADMIN-005-delete-user. |
| TC-ADMIN-026 | 2024-02-27 | ✅ Pass | Bug fixed and verified successfully. |
| TC-ADMIN-028 | 2024-03-01 | ❌ Fail | Bug reported as BUG-ADMIN-006-delete-all-users. |
| TC-ADMIN-028 | 2024-03-03 | ✅ Pass | Bug fixed and verified successfully. | |
