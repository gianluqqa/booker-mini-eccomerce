# Manual Test Cases - Orders Module

| ID | Module | Title | Priority | Preconditions | Steps | Test Data | Expected Result | Status |
| -- | ------ | ------ | --------- | -------------- | ----- | --------------- | ------------------ | ------ |
| TC-ORD-001 | Orders | Get order details by ID successfully | High | User authenticated, order exists and belongs to user | 1. Send GET /orders/:orderId with valid token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify order details including items<br>5. Verify order belongs to authenticated user | Valid token, orderId: valid_order_id (belongs to user) | HTTP 200, success: true, order with correct id, status, total, items array with book details | |
| TC-ORD-002 | Orders | Reject getting non-existent order | High | User authenticated, order does not exist in database | 1. Send GET /orders/:nonExistentOrderId with valid token<br>2. Verify HTTP 404 response<br>3. Verify order not found error message | Valid token, orderId: non_existent_uuid | HTTP 404, success: false, message: "Orden no encontrada" | |
| TC-ORD-003 | Orders | Reject viewing another user's order (Security) | High | User authenticated, attempting to view different user's order | 1. Send GET /orders/:anotherUserOrderId with authenticated user's token<br>2. Verify HTTP 403 response<br>3. Verify permission error message | Authenticated token, orderId: belongs_to_different_user | HTTP 403, success: false, message: "No tienes permiso para ver esta orden" | |
| TC-ORD-004 | Orders | Handle invalid UUID format for order ID | Medium | User authenticated, invalid UUID format provided | 1. Send GET /orders/:invalidUuid with valid token<br>2. Verify HTTP 500 response<br>3. Verify error message | Valid token, orderId: "id-invalido-123" | HTTP 500, success: false, message: "Error al obtener la orden" | |
| TC-ORD-005 | Orders | Get empty confirmed orders list | Medium | User authenticated, no confirmed orders exist | 1. Send GET /orders/ with valid token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify orders array is empty | Valid token | HTTP 200, success: true, data: [] | |
| TC-ORD-006 | Orders | Get user's confirmed orders list | High | User authenticated, confirmed orders exist for user | 1. Create PAID order for user<br>2. Send GET /orders/ with valid token<br>3. Verify HTTP 200 response<br>4. Verify success is true<br>5. Verify only PAID orders are returned<br>6. Verify orders belong to authenticated user | Valid token, user with PAID order | HTTP 200, success: true, data array contains user's PAID orders only | |
| TC-ORD-007 | Orders | Reject getting orders without authentication | High | No authentication token provided | 1. Send GET /orders/ without token<br>2. Verify HTTP 401 response<br>3. Verify authorization error message | No token | HTTP 401, success: false, message: "No autorizado: se requiere un token de autenticación" | |
| TC-ORD-008 | Orders | Reject getting orders with invalid token | High | Invalid authentication token provided | 1. Send GET /orders/ with invalid token<br>2. Verify HTTP 401 response<br>3. Verify invalid token error message | Token: "token-super-invalido" | HTTP 401, success: false, message: "No autorizado: token inválido o expirado" | |
| TC-ORD-009 | Orders | Exclude PENDING and CANCELLED orders from confirmed list | High | User authenticated, orders exist in different statuses | 1. Create PAID, PENDING, and CANCELLED orders for user<br>2. Send GET /orders/ with valid token<br>3. Verify HTTP 200 response<br>4. Verify only PAID order is returned<br>5. Verify PENDING and CANCELLED are excluded | Valid token, user with PAID, PENDING, CANCELLED orders | HTTP 200, success: true, data contains only PAID order, excludes PENDING and CANCELLED | |
| TC-ORD-010 | Orders | Ensure privacy: not return other users' confirmed orders | High | User authenticated, orders exist for multiple users | 1. Create PAID order for authenticated user<br>2. Create PAID order for different user<br>3. Send GET /orders/ with authenticated user's token<br>4. Verify only authenticated user's order is returned | Valid token, mainUserOrder, otherUserOrder | HTTP 200, success: true, data contains only main user's order, excludes other user's order | |
| TC-ORD-011 | Orders | Get empty pending orders list | Medium | User authenticated, no pending orders exist | 1. Send GET /orders/pending with valid token<br>2. Verify HTTP 200 response<br>3. Verify success is true<br>4. Verify pending orders array is empty | Valid token | HTTP 200, success: true, data: [] | |
| TC-ORD-012 | Orders | Get user's pending order | High | User authenticated, PENDING order exists for user | 1. Create PENDING order for user<br>2. Send GET /orders/pending with valid token<br>3. Verify HTTP 200 response<br>4. Verify success is true<br>5. Verify PENDING order is returned<br>6. Verify order belongs to authenticated user | Valid token, user with PENDING order | HTTP 200, success: true, data array contains user's PENDING order | |
| TC-ORD-013 | Orders | Reject getting pending orders without authentication | High | No authentication token provided | 1. Send GET /orders/pending without token<br>2. Verify HTTP 401 response<br>3. Verify authorization error message | No token | HTTP 401, success: false, message: "No autorizado: se requiere un token de autenticación" | |
| TC-ORD-014 | Orders | Reject getting pending orders with invalid token | High | Invalid authentication token provided | 1. Send GET /orders/pending with invalid token<br>2. Verify HTTP 401 response<br>3. Verify invalid token error message | Token: "token-super-invalido" | HTTP 401, success: false, message: "No autorizado: token inválido o expirado" | |
| TC-ORD-015 | Orders | Exclude PAID and CANCELLED orders from pending list | High | User authenticated, orders exist in different statuses | 1. Create PAID, PENDING, and CANCELLED orders for user<br>2. Send GET /orders/pending with valid token<br>3. Verify HTTP 200 response<br>4. Verify only PENDING order is returned<br>5. Verify PAID and CANCELLED are excluded | Valid token, user with PAID, PENDING, CANCELLED orders | HTTP 200, success: true, data contains only PENDING order, excludes PAID and CANCELLED | |
| TC-ORD-016 | Orders | Ensure privacy: not return other users' pending orders | High | User authenticated, pending orders exist for multiple users | 1. Create PENDING order for authenticated user<br>2. Create PENDING order for different user<br>3. Send GET /orders/pending with authenticated user's token<br>4. Verify only authenticated user's pending order is returned | Valid token, mainUserPendingOrder, otherUserPendingOrder | HTTP 200, success: true, data contains only main user's PENDING order, excludes other user's PENDING order | |
