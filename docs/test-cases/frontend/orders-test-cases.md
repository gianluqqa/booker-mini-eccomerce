# Manual Test Cases - Orders Module (Frontend)

| ID | Module | Title | Priority | Preconditions | Steps | Test Data | Expected Result | Status |
| -- | ------ | ------ | --------- | -------------- | ----- | --------------- | ------------------ | ------ |
| TC-FRONT-ORDERS-001 | Orders List | Display user orders in profile | High | User authenticated, has orders | 1. Login as user<br>2. Navigate to /profile<br>3. Verify orders section displayed<br>4. Verify all orders shown | User with existing orders | All orders displayed with correct data | |
| TC-FRONT-ORDERS-002 | Orders List | Show loading state while loading orders | Medium | User authenticated | 1. Login as user<br>2. Navigate to /profile<br>3. Verify loading indicator shown<br>4. Verify loading disappears after orders load | User with orders | Loading indicator shown, disappears after load | |
| TC-FRONT-ORDERS-003 | Orders List | Show empty state for user without orders | Medium | User authenticated, no orders | 1. Login as user with no orders<br>2. Navigate to /profile<br>3. Verify empty state message<br>4. Verify "Start shopping" button shown | User without orders | Empty state message shown, shopping button visible | |
| TC-FRONT-ORDERS-004 | Orders List | Show error message if orders load fails | High | User authenticated, API failure | 1. Login as user<br>2. Navigate to /profile<br>3. Simulate API failure<br>4. Verify error message shown | User, API failure | Error message displayed | |
| TC-FRONT-ORDERS-005 | Orders List | Display order status badges | High | User authenticated, has orders | 1. Login as user<br>2. Navigate to /profile<br>3. Verify status badges displayed<br>4. Verify correct colors for each status | User with orders in different states | Status badges shown with correct colors (paid, pending, expired, cancel) | |
| TC-FRONT-ORDERS-006 | Orders List | Display order details in card | High | User authenticated, has orders | 1. Login as user<br>2. Navigate to /profile<br>3. Verify each order shows: ID, date, total, status, items | User with orders | All order details displayed correctly | |
| TC-FRONT-ORDERS-007 | Orders List | Display order items preview | Medium | User authenticated, has orders | 1. Login as user<br>2. Navigate to /profile<br>3. Verify order items shown with titles and quantities | User with orders | Items displayed with book titles and quantities | |
| TC-FRONT-ORDERS-008 | Orders List | Filter orders by status | Medium | User authenticated, has orders | 1. Login as user<br>2. Navigate to /profile<br>3. Select status filter<br>4. Verify only orders with selected status shown | User with orders in different states | Table shows only filtered orders | |
| TC-FRONT-ORDERS-009 | Order Details | View order details modal | High | User authenticated, has order | 1. Login as user<br>2. Navigate to /profile<br>3. Click on order to view details<br>4. Verify order details modal shown | User with order ID | Order details modal shows complete order information | |
| TC-FRONT-ORDERS-010 | Order Details | Display complete order information | High | User authenticated, order details modal开放 | 1. Open order details modal<br>2. Verify all info shown: items, subtotal, tax, total, dates | Order with items | Complete order information displayed | |
| TC-FRONT-ORDERS-011 | Order Details | Close order details modal | Medium | Order details modal open | 1. Open order details modal<br>2. Click close button<br>3. Verify modal closes | Order details modal open | Modal closes successfully | |
| TC-FRONT-ORDERS-012 | Pending Orders | Display pending order alert | High | User authenticated, has PENDING order | 1. User has PENDING order<br>2. Navigate to /profile<br>3. Verify PendingOrderAlert shown<br>4. Verify alert shows order details | User with PENDING order | PendingOrderAlert shows order ID, total, expiresAt | |
| TC-FRONT-ORDERS-013 | Pending Orders | Navigate to checkout from pending alert | Medium | User authenticated, has PENDING order | 1. User has PENDING order<br>2. Navigate to /profile<br>3. Click "Go to checkout" in alert<br>4. Verify redirect to /checkout | User with PENDING order | Redirect to /checkout with order ID | |
| TC-FRONT-ORDERS-014 | Pending Orders | Cancel pending order from alert | Medium | User authenticated, has PENDING order | 1. User has PENDING order<br>2. Navigate to /profile<br>3. Click "Cancel order" in alert<br>4. Verify order cancelled<br>5. Verify alert disappears | User with PENDING order | Order cancelled, alert removed, stock restored | |
| TC-FRONT-ORDERS-015 | Order Expiration | Show expired order status | High | User authenticated, has expired order | 1. User has expired PENDING order<br>2. Navigate to /profile<br>3. Verify order shows "expired" status<br>4. Verify status badge color | User with expired order | Status: "expired", correct badge color | |
| TC-FRONT-ORDERS-016 | Order Expiration | Handle expired order in alert | Medium | User authenticated, has expired PENDING order | 1. User has expired PENDING order<br>2. Navigate to /profile<br>3. Verify alert shows expired state<br>4. Verify "Start new checkout" button shown | User with expired order | Alert shows expired state, button to restart | |
| TC-FRONT-ORDERS-017 | Order Sorting | Display orders sorted by date (newest first) | Low | User authenticated, has multiple orders | 1. Login as user with multiple orders<br>2. Navigate to /profile<br>3. Verify orders sorted correctly | User with orders from different dates | Orders displayed newest first | |
| TC-FRONT-ORDERS-018 | Order Pagination | Handle large order lists | Low | User authenticated, has many orders | 1. Login as user with many orders (> limit)<br>2. Navigate to /profile<br>3. Verify pagination controls shown<br>4. Navigate between pages | User with 20+ orders | Pagination works, correct orders per page | |
| TC-FRONT-ORDERS-019 | Order Items | Display book images in order items | Medium | User authenticated, order has items with images | 1. Login as user<br>2. Navigate to /profile<br>3. View order details<br>4. Verify book images shown | Order with items that have images | Book images displayed correctly | |
| TC-FRONT-ORDERS-020 | Order Items | Display placeholder for missing images | Medium | User authenticated, order has items without images | 1. Login as user<br>2. Navigate to /profile<br>3. View order details<br>4. Verify placeholder icons shown | Order with items without images | Package icon (Lucide) shown as placeholder | |

---

## Execution History

| Test Case ID | Date | Result | Notes |
|---------------|------|--------|-------|
