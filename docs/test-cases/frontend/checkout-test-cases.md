# Manual Test Cases - Checkout Module (Frontend)

| ID | Module | Title | Priority | Preconditions | Steps | Test Data | Expected Result | Status |
| -- | ------ | ------ | --------- | -------------- | ----- | --------------- | ------------------ | ------ |
| TC-FRONT-CHECKOUT-001 | Checkout Initialization | Initialize checkout with cart items | High | User authenticated, cart has items, no PENDING order | 1. Add items to cart<br>2. Navigate to /checkout<br>3. Verify stock reservation created<br>4. Verify PENDING order created<br>5. Verify items displayed | Cart with items | Stock reservation created, PENDING order with expiresAt, items displayed | |
| TC-FRONT-CHECKOUT-002 | Checkout Initialization | Redirect to cart if cart empty | High | User authenticated, cart empty | 1. Navigate to /checkout with empty cart<br>2. Verify redirect to /cart | Empty cart | Redirect to /cart | |
| TC-FRONT-CHECKOUT-003 | Checkout Initialization | Load existing PENDING order | High | User authenticated, has PENDING order | 1. User has PENDING order<br>2. Navigate to /checkout<br>3. Verify existing order loaded<br>4. Verify order items displayed | User with PENDING order | Existing PENDING order loaded, items shown, expiresAt displayed | |
| TC-FRONT-CHECKOUT-004 | Checkout Initialization | Detect expired order on load | High | User authenticated, has expired PENDING order | 1. User has expired PENDING order<br>2. Navigate to /checkout<br>3. Verify orderExpired state set to true<br>4. Verify error message shown | User with expired PENDING order | orderExpired: true, error: "Tu orden ha expirado" | |
| TC-FRONT-CHECKOUT-005 | Checkout Initialization | Load specific order by ID | Medium | User authenticated, valid orderId in URL | 1. Navigate to /checkout/[orderId]<br>2. Verify specific order loaded<br>3. Verify order status is PENDING | Valid PENDING orderId | Specific order loaded, items displayed | |
| TC-FRONT-CHECKOUT-006 | Checkout Initialization | Redirect if order not PENDING | Medium | User authenticated, orderId with non-PENDING status | 1. Navigate to /checkout/[orderId] with PAID order<br>2. Verify redirect to /profile<br>3. Verify error message | Order with status PAID | Redirect to /profile, error: "Esta orden ya no está pendiente" | |
| TC-FRONT-CHECKOUT-007 | Checkout Initialization | Show loading state during initialization | Medium | User authenticated | 1. Navigate to /checkout<br>2. Verify loading indicator shown<br>3. Verify loading disappears after initialization | Any checkout | Loading state shown during API calls, disappears after | |
| TC-FRONT-CHECKOUT-008 | Payment Processing | Process payment successfully | High | User authenticated, PENDING order, valid payment data | 1. Navigate to /checkout with PENDING order<br>2. Fill payment form with valid data<br>3. Click "Confirm Pago"<br>4. Verify payment processed<br>5. Verify order status PAID<br>6. Verify redirect to profile | Valid card data: cardNumber, cardName, expiryDate, cvc | Payment successful, order.status: "paid", redirect to /profile | |
| TC-FRONT-CHECKOUT-009 | Payment Processing | Frontend validation for empty card number | High | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Leave card number empty<br>3. Click "Confirm Pago"<br>4. Verify validation error | cardNumber: "" | Validation error: "El número de tarjeta es obligatorio" | |
| TC-FRONT-CHECKOUT-010 | Payment Processing | Frontend validation for invalid card number | High | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Enter invalid card number format<br>3. Click "Confirm Pago"<br>4. Verify validation error | cardNumber: "123-456-789-235-" | Validation error: "Número de tarjeta inválido" | |
| TC-FRONT-CHECKOUT-011 | Payment Processing | Frontend validation for empty card name | High | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Leave card name empty<br>3. Click "Confirm Pago"<br>4. Verify validation error | cardName: "" | Validation error: "El nombre en la tarjeta es obligatorio" | |
| TC-FRONT-CHECKOUT-012 | Payment Processing | Frontend validation for card name format | Medium | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Enter card name with numbers/special chars<br>3. Verify input filtered automatically | cardName: "Juan123" | Input filtered to only letters and spaces | |
| TC-FRONT-CHECKOUT-013 | Payment Processing | Frontend validation for empty expiry date | High | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Leave expiry date empty<br>3. Click "Confirm Pago"<br>4. Verify validation error | expiryDate: "" | Validation error: "La fecha de vencimiento es obligatoria" | |
| TC-FRONT-CHECKOUT-014 | Payment Processing | Frontend validation for invalid expiry date | High | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Enter invalid expiry format<br>3. Click "Confirm Pago"<br>4. Verify validation error | expiryDate: "13/25" | Validation error: "Fecha de vencimiento inválida" | |
| TC-FRONT-CHECKOUT-015 | Payment Processing | Frontend validation for empty CVC | High | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Leave CVC empty<br>3. Click "Confirm Pago"<br>4. Verify validation error | cvc: "" | Validation error: "El CVC es obligatorio" | |
| TC-FRONT-CHECKOUT-016 | Payment Processing | Frontend validation for CVC length | Medium | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Enter CVC with less than 3 digits<br>3. Click "Confirm Pago"<br>4. Verify validation error | cvc: "12" | Validation error: "El CVC debe tener 3 o 4 dígitos" | |
| TC-FRONT-CHECKOUT-017 | Payment Processing | Show processing state during payment | High | User authenticated, PENDING order | 1. Fill payment form<br>2. Click "Confirm Pago"<br>3. Verify button shows loading state<br>4. Verify button disabled during processing | Valid payment data | Button shows "Procesando..." with spinner, disabled | |
| TC-FRONT-CHECKOUT-018 | Payment Processing | Show backend validation errors | High | User authenticated, PENDING order | 1. Fill payment form with invalid data (passes frontend)<br>2. Click "Confirm Pago"<br>3. Verify backend errors displayed | Invalid card data that passes frontend | Backend validation errors shown in fieldErrors | |
| TC-FRONT-CHECKOUT-019 | Payment Processing | Reject payment if order expired | High | User authenticated, expired PENDING order | 1. Navigate to /checkout with expired order<br>2. Attempt to fill payment form<br>3. Click "Confirm Pago"<br>4. Verify error message | Expired PENDING order | Error: "Tu orden ha expirado. Por favor, inicia un nuevo checkout" | |
| TC-FRONT-CHECKOUT-020 | Payment Processing | Reject payment if no PENDING order | High | User authenticated, no PENDING order | 1. Navigate to /checkout without PENDING order<br>2. Attempt to process payment<br>3. Verify error message | No PENDING order | Error: "No hay una orden válida para procesar el pago" | |
| TC-FRONT-CHECKOUT-021 | Checkout Cancellation | Cancel checkout successfully | High | User authenticated, PENDING order | 1. Navigate to /checkout with PENDING order<br>2. Click "Cancelar Checkout"<br>3. Verify order cancelled<br>4. Verify redirect to /cart<br>5. Verify stock restored | PENDING order | Order cancelled, redirect to /cart, stock restored | |
| TC-FRONT-CHECKOUT-022 | Checkout Cancellation | Show error if cancellation fails | Medium | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Click "Cancelar Checkout"<br>3. Simulate cancellation failure<br>4. Verify error message shown | PENDING order, cancellation fails | Error message displayed, order not cancelled | |
| TC-FRONT-CHECKOUT-023 | Checkout Restart | Restart checkout after expiration | High | User authenticated, expired PENDING order | 1. Navigate to /checkout with expired order<br>2. Click "Volver a la Tienda"<br>3. Verify expired order cancelled<br>4. Verify redirect to store | Expired PENDING order | Expired order cancelled, redirect to / (store) | |
| TC-FRONT-CHECKOUT-024 | Checkout Restart | Show processing state during restart | Medium | User authenticated, expired PENDING order | 1. Navigate to /checkout with expired order<br>2. Click "Volver a la Tienda"<br>3. Verify button shows loading state<br>4. Verify button disabled | Expired PENDING order | Button shows "Procesando..." with spinner, disabled | |
| TC-FRONT-CHECKOUT-025 | Order Expiration Timer | Display countdown timer | High | User authenticated, PENDING order with expiresAt | 1. Navigate to /checkout with PENDING order<br>2. Verify timer shows remaining time<br>3. Verify timer updates every second | PENDING order with expiresAt | Timer shows MM:SS format, updates every second | |
| TC-FRONT-CHECKOUT-026 | Order Expiration Timer | Show warning when time < 2 minutes | Medium | User authenticated, PENDING order with < 2 min remaining | 1. Navigate to /checkout with order expiring soon<br>2. Verify timer color changes to warning<br>3. Verify warning message shown | PENDING order with < 2 min remaining | Timer color changes, warning message displayed | |
| TC-FRONT-CHECKOUT-027 | Order Expiration Timer | Show expired state when time reaches 0 | High | User authenticated, PENDING order | 1. Navigate to /checkout with PENDING order<br>2. Wait for timer to reach 0<br>3. Verify expired state shown<br>4. Verify error message | PENDING order with expiresAt | Timer shows "Expirado", error: "Tu orden ha expirado" | |
| TC-FRONT-CHECKOUT-028 | Order Expiration Timer | Clear interval on component unmount | Medium | User authenticated, PENDING order | 1. Navigate to /checkout with timer active<br>2. Navigate away from page<br>3. Verify interval cleared (no memory leak) | PENDING order with active timer | Interval cleared, no console errors | |
| TC-FRONT-CHECKOUT-029 | Cart Preview | Display cart items in checkout | High | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Verify CartPreview shows order items<br>3. Verify item details correct | PENDING order with items | Items displayed with title, author, price, quantity | |
| TC-FRONT-CHECKOUT-030 | Cart Preview | Handle missing book images | Medium | User authenticated, PENDING order with items without images | 1. Navigate to /checkout with items without images<br>2. Verify placeholder icons shown | Items without images | Package icon (Lucide) shown as placeholder | |
| TC-FRONT-CHECKOUT-031 | Checkout Totals | Calculate subtotal correctly | High | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Verify subtotal calculated from order items<br>3. Verify subtotal = sum(price * quantity) | PENDING order with items | Subtotal matches sum of (price * quantity) | |
| TC-FRONT-CHECKOUT-032 | Checkout Totals | Calculate tax correctly | Medium | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Verify tax calculated (21% of subtotal)<br>3. Verify tax = subtotal * 0.21 | PENDING order with items | Tax = subtotal * 0.21 | |
| TC-FRONT-CHECKOUT-033 | Checkout Totals | Calculate total correctly | High | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Verify total = subtotal + tax<br>3. Verify total matches order total | PENDING order with items | Total = subtotal + tax, matches order.total | |
| TC-FRONT-CHECKOUT-034 | Checkout Actions | Show correct button based on order state | High | User authenticated | 1. Navigate to /checkout with active order<br>2. Verify "Confirm Pago" button shown<br>3. Wait for order to expire<br>4. Verify "Volver a la Tienda" button shown | Active then expired order | Active: "Confirm Pago", Expired: "Volver a la Tienda" | |
| TC-FRONT-CHECKOUT-035 | Checkout Actions | Disable buttons during processing | High | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Click "Confirm Pago"<br>3. Verify all buttons disabled during processing<br>4. Verify buttons re-enabled after completion | PENDING order | Buttons disabled during processing, re-enabled after | |
| TC-FRONT-CHECKOUT-036 | Checkout Actions | Cancel button always available | Medium | User authenticated, PENDING order | 1. Navigate to /checkout<br>2. Verify "Cancelar Checkout" button shown<br>3. Verify button works in both active and expired states | PENDING order (active or expired) | "Cancelar Checkout" button always shown and functional | |
| TC-FRONT-CHECKOUT-037 | Error Handling | Show error state on initialization failure | High | User authenticated, initialization fails | 1. Navigate to /checkout<br>2. Simulate initialization failure<br>3. Verify error message shown<br>4. Verify error component displayed | Initialization failure | Error message shown, error state displayed | |
| TC-FRONT-CHECKOUT-038 | Error Handling | Clear field errors on user interaction | Medium | User authenticated, PENDING order | 1. Fill payment form with errors<br>2. Click "Confirm Pago"<br>3. Verify field errors shown<br>4. Interact with field<br>5. Verify field error cleared | Payment form with errors | Field error clears when user interacts with field | |
| TC-FRONT-CHECKOUT-039 | Checkout Flow | Complete full checkout flow | High | User authenticated, cart with items | 1. Add items to cart<br>2. Navigate to /checkout<br>3. Verify order created automatically<br>4. Fill payment form<br>5. Process payment<br>6. Verify success and redirect | Cart with items, valid payment data | Full flow completes successfully, order PAID, redirect to profile | |
| TC-FRONT-CHECKOUT-040 | Checkout Flow | Handle concurrent order creation | Medium | User authenticated | 1. Navigate to /checkout in two tabs<br>2. Verify only one PENDING order created<br>3. Verify second tab loads existing order | Same user, two tabs | Second tab loads existing PENDING order (idempotency) | |

---

## Execution History

| Test Case ID | Date | Result | Notes |
|---------------|------|--------|-------|
