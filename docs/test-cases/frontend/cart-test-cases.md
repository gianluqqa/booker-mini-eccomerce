# Manual Test Cases - Cart Module (Frontend)

| ID | Module | Title | Priority | Preconditions | Steps | Test Data | Expected Result | Status |
| -- | ------ | ------ | --------- | -------------- | ----- | --------------- | ------------------ | ------ |
| TC-FRONT-CART-001 | Cart | Display empty cart | Medium | User authenticated, cart is empty | 1. Navigate to /cart<br>2. Verify empty cart message<br>3. Verify cart shows 0 items and 0 total | User with empty cart | Shows "Tu carrito está vacío" message, totalItems: 0, totalPrice: 0 | |
| TC-FRONT-CART-002 | Cart | Display cart with items | High | User authenticated, cart has items | 1. Add books to cart<br>2. Navigate to /cart<br>3. Verify all items displayed<br>4. Verify totals calculated correctly | Multiple books with different quantities | All items shown, correct totalItems and totalPrice | |
| TC-FRONT-CART-003 | Cart | Add book to cart successfully | High | User authenticated, book exists with stock | 1. Navigate to book detail page<br>2. Click "Add to cart" button<br>3. Verify success message<br>4. Verify cart count updates | bookId: valid_book_id, quantity: 1 | Success message shown, cart count increases | |
| TC-FRONT-CART-004 | Cart | Add book with custom quantity | Medium | User authenticated, book exists with stock | 1. Navigate to book detail page<br>2. Select quantity > 1<br>3. Click "Add to cart"<br>4. Verify correct quantity added | bookId: valid_book_id, quantity: 3 | Book added with quantity 3 | |
| TC-FRONT-CART-005 | Cart | Accumulate quantity if book already in cart | Medium | User authenticated, book already in cart | 1. Add book with quantity 1<br>2. Add same book again with quantity 2<br>3. Verify total quantity is 3 | Same book added twice | Final quantity: 3 (1 + 2) | |
| TC-FRONT-CART-006 | Cart | Reject adding if insufficient stock | High | User authenticated, book has limited stock | 1. Attempt to add book with quantity > stock<br>2. Verify error message<br>3. Verify cart not updated | bookId: valid_book_id (stock: 5), quantity: 10 | Error message: "Stock insuficiente", cart unchanged | |
| TC-FRONT-CART-007 | Cart | Update item quantity successfully | High | User authenticated, item in cart | 1. Navigate to /cart<br>2. Click + button to increase quantity<br>3. Verify quantity updates<br>4. Verify total price updates | cartId: valid_cart_id, new quantity: 5 | Quantity updated to 5, total price recalculated | |
| TC-FRONT-CART-008 | Cart | Decrement item quantity | Medium | User authenticated, item with quantity > 1 | 1. Navigate to /cart<br>2. Click - button to decrease quantity<br>3. Verify quantity decreases<br>4. Verify total price updates | cartId: valid_cart_id, quantity from 3 to 2 | Quantity updated to 2, total price recalculated | |
| TC-FRONT-CART-009 | Cart | Remove item when quantity reaches 0 | High | User authenticated, item with quantity 1 | 1. Navigate to /cart<br>2. Click - button on item with quantity 1<br>3. Verify item removed from cart<br>4. Verify totals updated | cartId: valid_cart_id, quantity: 1 | Item removed, totals recalculated | |
| TC-FRONT-CART-010 | Cart | Reject quantity update if exceeds stock | High | User authenticated, item in cart, stock limited | 1. Attempt to increase quantity beyond available stock<br>2. Verify alert message<br>3. Verify quantity not updated | cartId: valid_cart_id, stock: 5, attempt quantity: 10 | Alert: "Solo hay X unidades disponibles", quantity unchanged | |
| TC-FRONT-CART-011 | Cart | Remove single item from cart | High | User authenticated, item in cart | 1. Navigate to /cart<br>2. Click trash icon on item<br>3. Verify item removed<br>4. Verify totals updated | cartId: valid_cart_id | Item removed, totals recalculated | |
| TC-FRONT-CART-012 | Cart | Clear all cart items | High | User authenticated, multiple items in cart | 1. Navigate to /cart<br>2. Click "Clear cart" button<br>3. Confirm in alert dialog<br>4. Verify all items removed | Cart with multiple items | All items removed, cart empty, totals: 0 | |
| TC-FRONT-CART-013 | Cart | Cancel clear cart action | Medium | User authenticated, items in cart | 1. Navigate to /cart<br>2. Click "Clear cart" button<br>3. Click cancel in alert dialog<br>4. Verify items remain | Cart with items | Alert dismissed, items unchanged | |
| TC-FRONT-CART-014 | Cart | Redirect to login if not authenticated | High | User not authenticated | 1. User not logged in<br>2. Navigate to /cart<br>3. Verify redirect to login | User not authenticated | Redirect to /login | |
| TC-FRONT-CART-015 | Cart | Block cart modifications if pending order exists | High | User authenticated, has PENDING order | 1. User has PENDING order<br>2. Navigate to /cart<br>3. Attempt to modify quantity<br>4. Verify alert message<br>5. Verify modification blocked | User with PENDING order | Alert: "No puedes modificar el carrito mientras tienes una orden pendiente", modification blocked | |
| TC-FRONT-CART-016 | Cart | Show pending order alert if exists | High | User authenticated, has PENDING order | 1. User has PENDING order<br>2. Navigate to /cart<br>3. Verify PendingOrderAlert component shown<br>4. Verify alert shows order details | User with PENDING order | PendingOrderAlert shows order ID, total, expiresAt | |
| TC-FRONT-CART-017 | Cart | Allow modifications if pending order expired | Medium | User authenticated, has EXPIRED PENDING order | 1. User has expired PENDING order<br>2. Navigate to /cart<br>3. Attempt to modify cart<br>4. Verify modification allowed | User with expired PENDING order | Modification allowed (order expired) | |
| TC-FRONT-CART-018 | Cart | Auto-increment quantity on long press (+) | Low | User authenticated, item in cart | 1. Navigate to /cart<br>2. Long press + button<br>3. Verify quantity auto-increments<br>4. Release button<br>5. Verify auto-increment stops | cartId: valid_cart_id | Quantity increases continuously while pressed, stops on release | |
| TC-FRONT-CART-019 | Cart | Auto-decrement quantity on long press (-) | Low | User authenticated, item with quantity > 1 | 1. Navigate to /cart<br>2. Long press - button<br>3. Verify quantity auto-decrements<br>4. Release button<br>5. Verify auto-decrement stops | cartId: valid_cart_id, quantity > 1 | Quantity decreases continuously while pressed, stops on release | |
| TC-FRONT-CART-020 | Cart | Show loading state during operations | Medium | User authenticated | 1. Navigate to /cart<br>2. Add item to cart<br>3. Verify loading indicator<br>4. Verify loading disappears after operation | Any cart operation | Loading indicator (Loader2) shows during operation, disappears after | |
| TC-FRONT-CART-021 | Cart | Show error message on operation failure | High | User authenticated, operation fails | 1. Navigate to /cart<br>2. Attempt operation that fails (network error)<br>3. Verify error message shown | Operation that fails | Error message displayed to user | |
| TC-FRONT-CART-022 | Cart | Navigate to checkout from cart | High | User authenticated, cart has items | 1. Navigate to /cart<br>2. Click "Proceed to checkout" button<br>3. Verify redirect to /checkout | Cart with items | Redirect to /checkout | |
| TC-FRONT-CART-023 | Cart | Navigate to store from empty cart | Medium | User authenticated, empty cart | 1. Navigate to /cart with empty cart<br>2. Click "Continue shopping" button<br>3. Verify redirect to store | Empty cart | Redirect to / (store) | |
| TC-FRONT-CART-024 | Cart | Display book image in cart item | Medium | User authenticated, item with image | 1. Add book with image to cart<br>2. Navigate to /cart<br>3. Verify book image displayed | Book with valid image URL | Book image shown in cart item | |
| TC-FRONT-CART-025 | Cart | Display placeholder if no book image | Medium | User authenticated, item without image | 1. Add book without image to cart<br>2. Navigate to /cart<br>3. Verify placeholder icon shown | Book without image | Package icon (Lucide) shown as placeholder | |
| TC-FRONT-CART-026 | Cart | Calculate totals correctly with multiple items | High | User authenticated, multiple items with different prices | 1. Add Book A (29.99, quantity 2) and Book B (15.0, quantity 1)<br>2. Navigate to /cart<br>3. Verify totalItems: 3<br>4. Verify totalPrice: 74.98 | Book A: 29.99 * 2 = 59.98, Book B: 15.0 * 1 = 15.0 | totalItems: 3, totalPrice: 74.98 | |
| TC-FRONT-CART-027 | Cart | Persist cart after page refresh | Medium | User authenticated, items in cart | 1. Add items to cart<br>2. Refresh page<br>3. Verify cart items persist | Items in cart | Cart items still present after refresh | |
| TC-FRONT-CART-028 | Cart | Sync with backend after external changes | Medium | User authenticated | 1. Add item to cart<br>2. Modify cart via another session/API<br>3. Refresh cart page<br>4. Verify cart shows updated data | External cart modification | Cart reflects backend state after refresh | |

---

## Execution History

| Test Case ID | Date | Result | Notes |
|---------------|------|--------|-------|
