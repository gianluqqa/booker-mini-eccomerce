# Bug Reports - Auth Module

| Bug ID | Title | Module | Status | Reported Date | Resolved Date | Related Test Case | Summary | URL | Screenshot | Steps to Reproduce | Expected Result | Actual Result | Severity | Priority | Assigned To |
| ------ | ----- | ------ | ------ | ------------- | ------------- | ----------------- | ------- | --- | ---------- | ------------------ | --------------- | ------------- | -------- | -------- | ----------- |
| BUG-AUTH-001-user-registration | El registro de usuario permite duplicar emails al no validar existencia previa en BD | Registration | Closed | 2024-01-15 | 2024-01-18 | TC-AUTH-003 | Al enviar una petición POST /users/register con un email ya registrado previamente en el sistema, el servidor no retorna error 409 y falla con un error no controlado de base de datos. | /users/register | N/A | 1. Registrar un usuario con email "existente@test.com".<br>2. Enviar nuevamente POST a /users/register con el mismo email "existente@test.com", name: "Nuevo", surname: "Usuario", password: "Password123!", confirmPassword: "Password123!".<br>3. Evaluar la respuesta devuelta por la API. | Respuesta HTTP 409 Conflict con { success: false, message: "Ya existe un usuario con ese email" }. | Error HTTP 500 Internal Server Error con excepción de unicidad (QueryFailedError) sin mensaje amigable al cliente. | High | High | N/A |
| BUG-AUTH-002-user-login | El login revela la existencia de emails al devolver error 404 en lugar de mensaje genérico | Login | Closed | 2024-01-20 | 2024-01-22 | TC-AUTH-014 | Al intentar iniciar sesión con un email no registrado, la API devuelve HTTP 404 con "Usuario no encontrado", exponiendo la existencia de emails registrados y rompiendo la regla de seguridad de credenciales inconsistentes. | /users/login | N/A | 1. Enviar una petición POST a /users/login con email "noexiste@test.com" y password "Password123!".<br>2. Observar el código de estado y el mensaje de la respuesta. | Respuesta HTTP 401 Unauthorized con { success: false, message: "Credenciales inválidas" }. | Respuesta HTTP 404 Not Found con mensaje "Usuario no encontrado". | High | High | N/A |
| BUG-AUTH-003-firebase-login | El login con Firebase no actualiza el estado de isNewUser para usuarios existentes | Firebase Login | Closed | 2024-01-25 | 2024-01-27 | TC-AUTH-021 | Al autenticarse vía POST /users/firebase-login con una cuenta de correo ya existente en el sistema, la propiedad isNewUser es retornada como true en lugar de false. | /users/firebase-login | N/A | 1. Registrar un usuario cliente con el email "existente@test.com".<br>2. Enviar POST a /users/firebase-login con el mismo email "existente@test.com".<br>3. Verificar el objeto de respuesta devuelto por la API. | Respuesta HTTP 200 OK con { success: true, message: "Autenticación con Firebase exitosa", isNewUser: false, data: { ... } }. | La respuesta incluye { success: true, isNewUser: true }, indicando erróneamente que se creó un usuario nuevo. | Medium | Medium | N/A |
| BUG-AUTH-004-jwt-token-generation | El token de autenticación devuelto en el login con Firebase carece de firma JWT válida | Firebase Login | Closed | 2024-01-30 | 2024-02-01 | TC-AUTH-028 | Al autenticarse mediante POST /users/firebase-login, el accessToken generado en la respuesta no sigue la estructura de 3 partes delimitadas por puntos propia de una firma JWT válida. | /users/firebase-login | N/A | 1. Enviar una petición POST a /users/firebase-login con un email válido.<br>2. Extraer el campo accessToken del objeto data de la respuesta.<br>3. Validar el formato JWT dividiendo la cadena por puntos (header.payload.signature). | El accessToken devuelto debe ser una cadena JWT válida estructurada en 3 partes separadas por puntos. | El accessToken devuelto es una cadena plana o token opaco no decodificable por los middlewares de autenticación del backend. | High | High | N/A |
