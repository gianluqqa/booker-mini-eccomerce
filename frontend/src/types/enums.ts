// Enums del backend para usar en el frontend

export enum UserRole {
  CUSTOMER = "customer",
  ADMIN = "admin",
}

export enum OrderStatus {
  PENDING = "pending",
  PAID = "paid",
  SHIPPED = "shipped",
  CANCELLED = "cancelled",
}

